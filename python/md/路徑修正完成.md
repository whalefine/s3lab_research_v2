# ✅ 資料集路徑修正完成

## 修正的路徑

### 問題
之前 UAV123_10fps 的路徑配置不正確，缺少了子目錄層級。

### 已修正
**檔案**: `lib/test/evaluation/local.py`

```python
# 修正前
settings.uav123_10fps_path = '/home/junjie/01_Research/SGLATrack-main/data/uav123_10fps'

# 修正後 ✓
settings.uav123_10fps_path = '/home/junjie/01_Research/SGLATrack-main/data/uav123_10fps/UAV123_10fps'
```

---

## 📊 當前資料集狀態

### ✅ 可用於訓練的資料集

| 資料集 | 路徑 | 狀態 |
|--------|------|------|
| **COCO17** | `data/coco` | ✓ 可用 (118,287 張訓練圖片) |
| **UAV123** | `data/uav123/UAV123` | ✓ 可用 (91 個視訊序列) |

### ✅ 可用於測試的資料集

| 資料集 | 路徑 | 狀態 |
|--------|------|------|
| **uav123** | `data/uav123/UAV123` | ✓ 可用 (123 標註, 91 序列) |
| **uav123_10fps** | `data/uav123_10fps/UAV123_10fps` | ✓ 可用 (123 標註, 91 序列) |

### ⚠️ 未配置的資料集 (非必需)

以下資料集路徑已配置但資料不存在（這些是可選的）：

**訓練用**:
- LaSOT
- GOT-10k
- TrackingNet

**測試用**:
- UAVDT
- DTB70
- UAVTrack112

如果需要這些資料集，請下載並放到對應路徑。

---

## 🔍 驗證路徑

執行驗證腳本確認所有路徑：

```bash
conda activate sgla
python check_dataset_paths.py
```

**預期輸出**:
```
✓ COCO: 可用
✓ UAV123 (訓練): 可用
✓ UAV123: 可用
✓ UAV123_10fps: 可用

當前可用於訓練的資料集:
  ✓ COCO17
  ✓ UAV123

當前可用於測試的資料集:
  ✓ uav123
  ✓ uav123_10fps
```

---

## 🚀 現在可以使用的訓練配置

### 配置 1: COCO + UAV123 (推薦)
```bash
python tracking/train.py --script sglatrack --config coco_uav123 \
  --save_dir ./output --mode multiple --nproc_per_node 4 --use_wandb 0
```

### 配置 2: 只用 COCO
```bash
python tracking/train.py --script sglatrack --config coco_only \
  --save_dir ./output --mode multiple --nproc_per_node 4 --use_wandb 0
```

---

## 🧪 測試命令

### 測試 UAV123
```bash
python tracking/test.py --tracker_param sglatrack --dataset uav123 \
  --threads 8 --num_gpus 1
```

### 測試 UAV123_10fps
```bash
python tracking/test.py --tracker_param sglatrack --dataset uav123_10fps \
  --threads 8 --num_gpus 1
```

---

## 📁 資料集結構確認

### COCO (✓ 正確)
```
data/coco/
├── annotations/
│   ├── instances_train2017.json  ✓
│   └── instances_val2017.json    ✓
└── images/
    ├── train2017/  (118,287 張)  ✓
    └── val2017/    (5,000 張)    ✓
```

### UAV123 (✓ 正確)
```
data/uav123/UAV123/
├── anno/UAV123/        (123 標註)  ✓
└── data_seq/UAV123/    (91 序列)   ✓
```

### UAV123_10fps (✓ 已修正)
```
data/uav123_10fps/UAV123_10fps/
├── anno/UAV123_10fps/      (123 標註)  ✓
└── data_seq/UAV123_10fps/  (91 序列)   ✓
```

---

## 📝 路徑配置檔案

所有路徑配置都在以下兩個檔案：

### 訓練路徑
**檔案**: `lib/train/admin/local.py`
```python
self.coco_dir = '/home/junjie/01_Research/SGLATrack-main/data/coco'
self.uav123_dir = '/home/junjie/01_Research/SGLATrack-main/data/uav123/UAV123'
```

### 測試路徑
**檔案**: `lib/test/evaluation/local.py`
```python
settings.uav123_path = '/home/junjie/01_Research/SGLATrack-main/data/uav123/UAV123'
settings.uav123_10fps_path = '/home/junjie/01_Research/SGLATrack-main/data/uav123_10fps/UAV123_10fps'
```

---

## 🎯 總結

✅ **所有您擁有的資料集路徑都已正確配置！**

您現在可以：
1. ✓ 使用 COCO + UAV123 訓練
2. ✓ 在 UAV123 測試集上測試
3. ✓ 在 UAV123_10fps 測試集上測試

所有路徑都指向正確的位置，可以直接開始訓練和測試！

---

## 🛠️ 工具腳本

- `check_dataset_paths.py` - 驗證所有資料集路徑
- `test_uav123_dataset.py` - 測試 UAV123 訓練資料集載入
- `check_workstation_config.py` - 完整工作站配置檢查

執行任何一個腳本都可以驗證配置狀態。

---

## 📋 檔案修改記錄

| 檔案 | 修改內容 | 狀態 |
|------|----------|------|
| `lib/test/evaluation/local.py` | 修正 uav123_10fps_path | ✅ 已修正 |
| `check_dataset_paths.py` | 新建路徑驗證腳本 | ✅ 新建 |

---

**所有路徑配置完成！可以開始訓練了！** 🚀
