# 🎨 圖表格式已更新 - 與參考圖片一致

## ✅ 已完成的修改

### 1. 圖例顯示完整資訊
**原本**: `sglatrack_deit_distilled [66.9]`  
**現在**: `sglatrack_deit_distilled_uav123 [66.9]`

包含了資料集名稱，與您的參考圖片格式一致！

### 2. 標題格式更新
**原本**: 
- Success
- Precision plot  
- Normalized Precision

**現在**: 
- Success Plot - uav123
- Precision Plot - uav123
- Normalized Precision - uav123

### 3. Y 軸範圍調整
為了與參考圖片一致：
- Success Plot: 0-100 (原本 0-88)
- Precision Plot: 0-100 (保持不變)
- Normalized Precision: 0-100 (原本 0-85)

---

## 🔧 修改的檔案

### 1. tracking/calculate_metrics.py (第 38-44 行)
```python
# 修改前
display_name=f'{tracker_name}_{param_name}'

# 修改後
display_name = f'{tracker_name}_{param_name}_{dataset_name}'
```

### 2. lib/test/analysis/plot_results.py (第 261-262 行)
```python
# Success Plot 標題
'title': f'Success Plot - {report_name}'

# Precision Plot 標題  
'title': f'Precision Plot - {report_name}'

# Normalized Precision 標題
'title': f'Normalized Precision - {report_name}'
```

---

## 📊 生成的圖表

### Success Plot
```
標題: Success Plot - uav123
圖例: sglatrack_deit_distilled_uav123 [66.9]
橫軸: Overlap threshold (0-1)
縱軸: Overlap Precision [%] (0-100)
```

### Precision Plot
```
標題: Precision Plot - uav123
圖例: sglatrack_deit_distilled_uav123 [84.9]
橫軸: Location error threshold [pixels] (0-50)
縱軸: Distance Precision [%] (0-100)
```

### Normalized Precision
```
標題: Normalized Precision - uav123
圖例: sglatrack_deit_distilled_uav123 [82.6]
橫軸: Location error threshold (0-0.5)
縱軸: Distance Precision [%] (0-100)
```

---

## 📊 與參考圖片對比

### 參考圖片格式
```
標題: Success Plot - uav123
圖例: sglatrack_deit_distilled_uav123_finetune [71.2]
```

### 您的圖表格式（現在）
```
標題: Success Plot - uav123
圖例: sglatrack_deit_distilled_uav123 [66.9]
```

### 差異說明
- ✅ 標題格式：完全一致
- ✅ 包含資料集名稱：完全一致
- ✅ 圖例位置：左下角（一致）
- ⚠️ 模型名稱：您的是 `deit_distilled`，參考圖是 `deit_distilled_uav123_finetune`
- ⚠️ AUC 分數：您的是 66.9，參考圖是 71.2（這是正常的，因為模型不同）

---

## 🎯 完整匹配參考圖片

如果您想要圖例完全匹配參考圖片（包含 `finetune`），可以修改：

### 選項 1: 在命令中指定自訂名稱
修改 `tracking/calculate_metrics.py` 增加參數：

```python
def calculate_metrics(tracker_name='sglatrack', param_name='deit_distilled', 
                     dataset_name='uav123', plot_curves=False, 
                     custom_name=None):  # 新增參數
    
    # 使用自訂名稱或預設格式
    if custom_name:
        display_name = custom_name
    else:
        display_name = f'{tracker_name}_{param_name}_{dataset_name}'
```

### 選項 2: 直接修改名稱
如果這是 finetune 模型，修改第 41 行：

```python
display_name = f'{tracker_name}_{param_name}_{dataset_name}_finetune'
```

---

## 🚀 使用方式

### 基本用法（已完成）
```bash
cd /home/junjie/01_Research/SGLATrack-main
conda activate sgla
python tracking/calculate_metrics.py --dataset uav123 --plot
```

### 結果
```
圖例: sglatrack_deit_distilled_uav123 [66.9]
標題: Success Plot - uav123
```

### 不同資料集
```bash
# UAV123_10fps
python tracking/calculate_metrics.py --dataset uav123_10fps --plot
# 圖例: sglatrack_deit_distilled_uav123_10fps [65.5]

# OTB
python tracking/calculate_metrics.py --dataset otb --plot
# 圖例: sglatrack_deit_distilled_otb [XX.X]
```

---

## 📂 檔案位置

```
output/test/result_plots/uav123/
├── success_plot.pdf           ✓ 已更新
├── precision_plot.pdf         ✓ 已更新
├── norm_precision_plot.pdf    ✓ 已更新
├── success_plot.tex
├── precision_plot.tex
└── norm_precision_plot.tex
```

---

## 📊 輸出結果

```
uav123                               | AUC    | OP50   | OP75   | Precision | Norm Precision | FPS    |
sglatrack_deit_distilled_uav123      | 66.90  | 82.23  | 60.15  | 84.91     | 82.56          | 205.85 |
```

**圖例格式**: `sglatrack_deit_distilled_uav123 [66.9]` ✓

---

## 🎨 格式對比表

| 元素 | 原本 | 現在 | 參考圖 |
|------|------|------|--------|
| **標題** | Success | **Success Plot - uav123** ✓ | Success Plot - uav123 |
| **圖例** | sglatrack_deit_distilled [66.9] | **sglatrack_deit_distilled_uav123 [66.9]** ✓ | sglatrack_deit_distilled_uav123_finetune [71.2] |
| **Y軸範圍** | 0-88 | **0-100** ✓ | 0-100 |
| **圖例字體** | 20 | **14** ✓ | ~14 |

---

## ✅ 總結

### 已完成
1. ✅ 圖例包含資料集名稱
2. ✅ 標題包含資料集名稱
3. ✅ Y 軸範圍擴展到 0-100
4. ✅ 圖例字體已縮小（14）
5. ✅ 格式與參考圖片一致

### 差異（正常）
- AUC 分數不同（66.9 vs 71.2）- 因為模型/訓練不同
- 模型後綴不同（無 `finetune`）- 根據您的實際模型命名

### 立即使用
```bash
python tracking/calculate_metrics.py --dataset uav123 --plot
```

**結果**: 圖表格式現在與您的參考圖片完全一致！🎉
