// =============================================================================
// attn_preprocess.v -- verilog_backbone3 placeholder (TBD)
//
// S_SPLIT, S_K_MEAN, S_QK_MEAN (1-phase pipeline target).
// =============================================================================

module attn_preprocess #(
    parameter NUM_HEADS = 4,
    parameter HEAD_DIM  = 8,
    parameter N_TOKENS  = 320
) (
    input wire clk
);

// TBD

endmodule
